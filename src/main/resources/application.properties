#define o profile padrão - dev / prod
#spring.profiles.active=dev

sistema.seguranca.enable-https=false
server.port=8080

#define como será chamada a api na url
server.servlet.context-path=/log-api

spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB


## default connection pool
spring.datasource.hikari.connectionTimeout=20000
spring.datasource.hikari.maximumPoolSize=5

# PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5433/log
spring.datasource.username=postgres
spring.datasource.password=root

#drop n create table again, good for testing, comment this in production
spring.jpa.hibernate.ddl-auto=create

spring.jpa.show-sql=true

spring.jackson.deserialization.fail-on-unknown-properties=true

spring.jackson.date-format=yyyy-MM-dd

spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults = false

spring.jpa.database-platform=org.hibernate.dialect.PostgreSQL9Dialect

#contato.disco.raiz=/media/development/7703F09C457F7BD53/Development/Workspace Intellij/jcsweb-api/foto
#contato.disco.diretorio-fotos=medico

##/tmp/contato-disco